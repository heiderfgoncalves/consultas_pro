version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: consultas_pro
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - consultas-pro-postgres:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - consultas-pro-redis:/data

volumes:
  consultas-pro-postgres:
  consultas-pro-redis:
